# CORS Configuration
# In production, set this to your frontend URL(s), comma-separated
# Example: https://your-app.vercel.app,https://your-app.netlify.app
ALLOWED_ORIGINS=*

# Jina Fallback (1=enabled, 0=disabled)
# Enable this to use Jina AI proxy when direct scraping fails
USE_JINA_FALLBACK=1

# Port (for local development)
# Most cloud platforms set this automatically
PORT=5000
