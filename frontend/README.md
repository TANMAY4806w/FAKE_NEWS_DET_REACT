# Fake News Frontend

Minimal React frontend for the Fake News Detection backend.

Quick start

1. cd frontend
2. npm install
3. npm start

The backend should be running on http://localhost:5000 (see `backend/app.py`).
